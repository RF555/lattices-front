/**
 * Tailwind CSS responsive breakpoints (pixels).
 * Single source of truth for both Tailwind config and JS hooks.
 */
export const BREAKPOINTS = {
  sm: 640,
  md: 768,
  lg: 1024,
  xl: 1280,
  '2xl': 1536,
} as const;
